pipeline{
	agent {
		docker {
			image 'ubuntu:v1_m'
		}
	}
	stages{
		stage('Build'){
			steps{
				sh''' cd $WORKSPACE && ls
				git pull origin main
				python2 file3.py
				pwd'''
			}
		}
	}
	post {
		failure {
				build job: 'job2', wait: true
			}
	}
}
